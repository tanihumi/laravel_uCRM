<script setup>
defineProps({
  blogs: Array
});

import { Link } from "@inertiajs/inertia-vue3";

</script>

<template>
<div v-if="$page.props.flash.message" class="bg-red-400">{{ $page.props.flash.message }}</div>
<ul>
  <li v-for="blog in blogs" :key="blog.id">件名： <Link class="text-blue-400" :href="route('inertia.show', { id: blog.id })">ルートパラメーター{{ blog.title }}</Link>、本文：{{ blog.content }}</li>
</ul>
aaaaaaaaaaaa
</template>
